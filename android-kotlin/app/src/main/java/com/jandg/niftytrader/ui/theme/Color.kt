package com.jandg.niftytrader.ui.theme

import androidx.compose.ui.graphics.Color

// ── Background layers ──────────────────────────────────────
val BgDeep    = Color(0xFF0A0F1E)   // darkest — page bg
val BgMid     = Color(0xFF111827)   // nav bar bg
val BgCard    = Color(0xFF141D2E)   // card surface
val BgBorder  = Color(0xFF1E2D45)   // card borders

// ── Brand accents ──────────────────────────────────────────
val Cyan      = Color(0xFF00D4AA)   // primary accent
val Orange    = Color(0xFFFF6B35)   // secondary accent
val Purple    = Color(0xFF7C3AED)   // tertiary

// ── Semantic ───────────────────────────────────────────────
val Gold      = Color(0xFFF59E0B)   // warning / caution
val Green     = Color(0xFF10B981)   // pass / profit
val Red       = Color(0xFFEF4444)   // fail / loss

// ── Text ───────────────────────────────────────────────────
val TextPrimary   = Color(0xFFE2E8F0)
val TextSecondary = Color(0xFF94A3B8)
val TextMuted     = Color(0xFF475569)
